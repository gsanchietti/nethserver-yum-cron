{
# We want to remove ${'yum-cron'}{'command'}
# in later release it can be removed
# introduced in version 0.1.7.el7
use esmith::ConfigDB;
my $db = esmith::ConfigDB->open;
$db->get_prop_and_delete('yum-cron', 'command');
}
